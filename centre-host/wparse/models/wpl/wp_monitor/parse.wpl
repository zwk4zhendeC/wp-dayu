package /wp_monitor/ {
    rule all {
        (
            #[copy_raw(name:"raw_log")]
            json | take(log) | json_unescape() | (
              chars:content\0
            )
        )
    }
}
