package /password/ {
    rule 操作日志 {
        (
            json | take(log) | json_unescape() | (
              time:date<[,]>,
              chars:obj<[,]>,
              chars:level<[,]>,
              chars:content\0
            )  
        )
    }
}
