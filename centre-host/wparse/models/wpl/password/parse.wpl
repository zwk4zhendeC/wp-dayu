#[copy_raw(name:"raw_log")]
package /password/ {
//    rule 操作日志 {
//        (
//            json | take(log) | json_unescape() | (
//              time:date<[,]>,
//              chars:obj<[,]>,
//              chars:level<[,]>,
//              chars:content\0
//            )  
//        )
//    }
    rule all {
        (
            json(
                  symbol(vaultwarden)@app_name
            )
        )
    }
}
